# Anima LoRA Training Configuration

[model_arguments]
dit_path = "/workspace/models/anima-base-v1.0.safetensors"
qwen3_path = "/workspace/models/qwen_3_06b_base.safetensors"
vae_path = "/workspace/models/qwen_image_vae.safetensors"

[dataset_arguments]
dataset_config = "/path/to/dataset.toml"
cache_latents_to_disk = true
cache_text_encoder_outputs = true
# train_data_dir is not needed when using dataset_config


[training_arguments]
output_dir = "./output/my_anima_lora"
output_name = "my_anima_lora"
save_model_as = "safetensors"
save_precision = "bf16"
max_train_epochs = 20
save_every_n_epochs = 1
logging_dir = "./output/my_anima_lora/logs"
log_with = "tensorboard"
learning_rate = 1e-4
text_encoder_lr = 0
optimizer_type = "AdamW8bit"
lr_scheduler = "cosine"
lr_warmup_steps = 50
mixed_precision = "bf16"
gradient_checkpointing = true
flash_attn = false
sdpa = true
max_grad_norm = 1.0
max_data_loader_n_workers = 4
persistent_data_loader_workers = true
seed = 42022

#[anima_arguments]
timestep_sample_method = "logit_normal"
discrete_flow_shift = 1.0

[sample_arguments]
sample_prompts = "./sample_prompts.txt"
sample_every_n_epochs = 1
sample_sampler = "euler_a"

[network_arguments]
network_module = "networks.lora_anima"
network_dim = 16
network_alpha = 16
network_train_unet_only = true
network_args = ["train_llm_adapter=False"]

# To initialize from a LoRA checkpoint (start new training with these weights):
# network_weights = "path\\to\\lora.safetensors"

# To resume training state (start from a saved state folder):
# resume = "path\\to\\saved_state_folder"

